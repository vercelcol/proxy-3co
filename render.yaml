services:
  - type: web
    name: secure-proxy
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TARGET_URL
        value: https://tu-servidor-real.com
      - key: NODE_ENV
        value: production
    healthCheckPath: /3co